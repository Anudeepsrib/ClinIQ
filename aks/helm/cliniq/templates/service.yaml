apiVersion: v1
kind: Service
metadata:
  name: {{ include "cliniq.fullname" . }}
  labels:
    {{- include "cliniq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cliniq.selectorLabels" . | nindent 4 }}
